# 模型卡（Model Card）

> 按实验要求写的初稿在开发前完成，这里是与最终代码一致的第 2 版。
> 最后更新：训练脚本运行时自动记录时间，见 `models/model_meta.json`。

---

## 一、基本信息

| 项 | 值 |
| --- | --- |
| 模型名称 | wine-classifier-app / model.joblib |
| 版本 | 1.0.0 |
| 任务类型 | **多分类**（不是回归） |
| 算法 | 逻辑回归 与 随机森林 二选一，由交叉验证主指标决定 |
| 主指标 | **macro F1** |
| 训练数据 | `sklearn.datasets.load_wine()`（UCI Wine，CC BY 4.0） |
| 样本量 | 178 |
| 特征数 | 13（全部数值型） |
| 类别数 | 3 |
| 运行环境 | 普通 CPU，不需要 GPU，不需要联网 |

## 二、用途（Intended Use）

- 课程实验 04 的演示：展示"训练 → 比较 → 保存 → 应用实时预测"的完整链路。
- 教学场景下的多分类 Pipeline 工程化示例。

## 三、非用途（Out-of-Scope / 明确不该用来做什么）

- ❌ **不能**用于任何真实的产品质量判定、贸易定价或合规决策。
- ❌ **不能**用于对真实酒类样品做化学鉴定。
- ❌ **不能**用于个体相关的决策（这是一份 1991 年左右的公开数据集，
  只有 178 条样本，不具备任何统计代表性）。
- ❌ 不要把这个模型当成"葡萄酒知识"——它只会根据 13 个化学指标
  在三个类别之间做划分，任何超出这个范围的问题它都答不了。

## 四、样本单位与标签

| 项 | 说明 |
| --- | --- |
| 一个样本是什么 | **一瓶酒的 13 项化学检测结果**（酒精含量、苹果酸、灰分等） |
| 标签是什么 | 该酒来源于哪一个栽培品种（3 类） |
| 标签来源 | 数据集自带标注，不是本人标注的 |
| 正类/负类 | 本任务是**多分类**，"正类/负类"的说法不适用；因此 Precision/Recall 一律用 `macro` 平均，并按类分别报告 |

## 五、评价协议

| 项 | 做法 |
| --- | --- |
| 划分方式 | **分层随机划分**（stratify），测试集占 25%，随机种子 42 |
| 为什么用分层 | 三类样本量是 59 / 71 / 48，不分层时小类别可能在测试集里只剩一两个，指标抖动大 |
| 为什么不分组划分 | 每行是一个独立的酒样本，不存在"同一主体多行"，所以不需要 GroupKFold |
| 模型选择 | 5 折分层交叉验证，看 macro F1 的均值，**不用测试集挑模型** |
| 测试集使用 | **只评一次**，写在 `src/xunlian.py` 的 `zui_zhong_pingjia()`，不在任何循环里 |
| 辅助指标 | Accuracy、Precision(macro/weighted)、Recall(macro/weighted)、F1(macro/weighted)、混淆矩阵、每类明细 |

**为什么会报两组指标**：`macro` 对所有类别一视同仁，适合看"小类有没有被牺牲"；
`weighted` 按样本量加权，更接近整体观感。只报一个容易误导，
所以实验要求里的"说明平均方式"就是这件事。

## 六、数据泄漏防控

| 风险点 | 处理方式 |
| --- | --- |
| 预处理器在划分前拟合 | 不发生在划分前；`StandardScaler` 包在 Pipeline 内，`cross_validate` 每折内部只在该折训练部分拟合 |
| 同一主体跨训练/测试集 | 不适用（每个样本互相独立），但已在 `docs/baseline.md` 说明判断依据 |
| 反复用测试集调参 | 代码结构上禁止：`zui_zhong_pingjia()` 只在 `shiji_xunlian()` 末尾调用一次 |
| 特征里含标签信息 | wine 的 13 个特征都是化学检测值，不含品种信息 |

## 七、指标体系与解释口径

| 指标 | 怎么读 |
| --- | --- |
| Accuracy | 整体判对比例。三类不均衡时会被大类带偏，不作为选模型依据 |
| Precision (macro) | 各类"说是这个类、真的是这个类"的比例取平均 |
| Recall (macro) | 各类"真是这个类、被找出来"的比例取平均 |
| F1 (macro) | 上两者的调和平均，**本实验的主指标** |
| 混淆矩阵 | 36 个测试样本的完整判定分布，能看出错在哪些类之间 |

**什么时候更看重 Precision / Recall**：
如果把酒按品种分档定价，**误报代价高**（把 A 当 B 卖出去），该更看重 Precision；
如果是筛查"有没有问题酒"，**漏报代价高**，该更看重 Recall。
本实验是课程演示，没有真实业务代价，所以用 F1 做平衡选择。

## 八、已知限制与风险

1. **样本量极小（178 条）**。指标的标准差比看上去大，
   交叉验证的 ±0.02 波动都不该被当成"模型 A 比 B 好"的结论。
2. **未做概率校准**。随机森林、逻辑回归的 `predict_proba` 都不是真实发生率；
   逻辑回归相对好一点，但没有验证过。界面上展示概率时已明确标注。
3. **不做分布外检测**。输入超出训练范围时模型照样给答案，
   **不报错、不提示**。这是当前版本最明显的短板。
   已用边界输入测试过（不会崩），但"不会崩" ≠ "结论可信"。
4. **只适用于这个数据集的 13 个特征**。换特征、换量纲、换品类都不行。
5. **标签只有 3 类且来源单一**，无法推广到其他酒类数据集。
6. **不做数据漂移监控**。数据分布变了模型不会知道。

## 九、伦理与合规

- 数据来自公开学术数据集（UCI），许可证 CC BY 4.0，已按要求记录来源。
- 不含个人信息、不含真实商业数据。
- 界面明确标注"仅用于课程演示"，避免被当真。
- **不做**任何基于酒精消费的预测或推荐，避免被用于不当用途。

## 十、重新训练的条件

出现下面任一情况就该重新训练，而不是继续用当前模型：

- 特征定义或单位发生变化（Schema 变了）。
- 数据来源换了一个批次，且分布明显不同（此时应先做分布对比）。
- 特征数量增减。此时 `model_meta.json` 里的特征顺序会不匹配，
  加载时会被 `jiazai_moxing()` 直接拒绝——这是**故意设计的**，
  宁可报错也不要静默给出错误预测。
- scikit-learn 主版本升级。跨主版本的 joblib 反序列化不保证兼容。

重新训练命令：

```powershell
.\.venv\Scripts\python src\xunlian.py
```

## 十一、训练与评价的复现方式

```powershell
python -m venv .venv
.\.venv\Scripts\python -m pip install -r requirements.txt
.\.venv\Scripts\python src\xunlian.py        # 训练，输出 models/
.\.venv\Scripts\python -m pytest tests -q   # 测试
streamlit run app.py                         # 启动界面
```

所有随机性都由 `SUIJI_ZHONGZI = 42` 固定，同环境重跑结果一致。
